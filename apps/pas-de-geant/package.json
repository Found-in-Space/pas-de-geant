{
  "name": "@found-in-space/pas-de-geant",
  "version": "0.1.0",
  "private": true,
  "description": "Walk around worlds, one small step at a time",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "dev": "vite --host 127.0.0.1",
    "preview": "vite preview --host 127.0.0.1",
    "prepare:relief": "node ./scripts/prepare-relief.mjs",
    "typecheck": "tsc --project tsconfig.json --pretty false"
  },
  "dependencies": {
    "@found-in-space/skykit": "0.2.0",
    "@found-in-space/three-star-field": "0.2.0",
    "@found-in-space/touch-os": "0.3.0",
    "@mapbox/martini": "0.2.0",
    "astronomy-engine": "2.1.19",
    "three": "0.185.1"
  },
  "devDependencies": {
    "@types/three": "0.185.1",
    "netcdfjs": "4.0.0"
  }
}
