# Changesets

Add a changeset for every user-visible package change:

```bash
npm run changeset
```

Choose the affected public package and the smallest appropriate semantic
version bump. The visualizer is private and is not versioned by Changesets.
